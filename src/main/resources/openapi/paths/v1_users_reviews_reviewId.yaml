delete:
  tags:
    - 마이-리뷰(43p)
  summary: 리뷰 삭제
  description: 44p 마이-리뷰 삭제
  security:
    - bearerAuth: [ ]
  parameters:
    - name: review_id
      in: path
      required: true
      description: 리뷰 아이디
      schema:
        $ref: '../schemas/reviewId.yaml'
  responses:
    "200":
      description: 성공
    default:
      description: Error
      content:
        application/json:
          schema:
            $ref: '../schemas/error.yaml'